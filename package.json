{
  "name": "riuka",
  "version": "2.0.0",
  "private": true,
  "description": "Riuka — Clash of Clans clan war-room dashboard: credential-holding relay server, Supabase-backed history, deterministic-first AI chat.",
  "main": "coc-local-proxy.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node coc-local-proxy.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5"
  }
}